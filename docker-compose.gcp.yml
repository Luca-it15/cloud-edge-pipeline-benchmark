name: cloud-edge-benchmark

# Compatibility override kept for older commands that still pass
# `-f docker-compose.yml -f docker-compose.gcp.yml`.
# The main compose file now runs the Google Cloud hybrid benchmark by default.
services: {}
